name: image_grid_view
version: 0.0.1
description: A customisable grid view for images, including resizing, cropping and selection.
homepage: https://github.com/byjtew/image_grid_view

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.11
  build_test: ^2.1.5
  build_web_compilers: ^3.2.3
  flutter_lints: ^2.0.1

